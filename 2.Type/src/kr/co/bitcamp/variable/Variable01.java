package kr.co.bitcamp.variable;
/*
 * 자료형 (DataTypes)
 * 
 * 1.기본형 (Primitive types) 
 * 우리가 스택영역에 만들어 놓은 공간에 2진법으로 변환된 실제 값이 그대로 저장
 * 정수형 : byte(1byte), short(2byte), int(4byte), long(8byte)
 * 실수형 : float(4byte), double(8byte)
 * 문자형 : char(2byte) -> 문자 한개 -> 참고로 문자열을 다루는 타입은 없다.
 * 논리형 : booleanlong(1byte)
 * 
 * 2.참조형(Referene types)
 * 스택 영역에 만들어 놓은 공간에 주고값이 저장이 된다.
 * 만약 해당 공간의 진자 값을 알고 싶다면 그 메모리 주소값을 참조해서 힙 영역에 찾아가야지 
 * 진짜 값을 찾을 수 있다.
 * 참조형 데이터타입에는 클래스형(class), 배열형(array), 인터페이스형(interface) 3가지 종류가 있음
 * 참조형 변수의 특징 -> 데이터가 저장된 메모리 주소 저장값을 저장하는 변수이다.
 */
public class Variable01 {
    
    
    
}
